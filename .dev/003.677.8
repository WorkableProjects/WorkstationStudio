Version 0.0.3.667.8

Install.sh —> Should ALSO (not replace) make a subdirectory and install the Workstation CLI there…. ~/CLI and run the command within it: git clone https://github.com/WorkableProjects/WorkstationCLI


Update README with correct information and install/start.sh 

—
	•	Remove the file manager unless you make it fully functional, a literal file manager similar to Windows and macOS.
	•	For the notes application, include Markdown formatting and have it save to a folder within the project and to browser storage.
	•	For tasks and to-dos, add a time element that creates a notification when the time arrives. Build a notifications component in the frontend that matches each theme.
	•	For the calculator, expand it beyond basic operations to a scientific calculator; if a full implementation is impractical, integrate Desmos instead.
	•	Finish adding animations, since some sections don’t have animations. (e.g., closing/minimzinng/maximizing apps)
	•	Improve the start menu: it currently feels too AI‑like, is center‑aligned, and includes icons. Align it left, remove the icons, and use clear, unique text. The menu should not feel Windows‑like; instead, it should have a distinctive Workstation Studio style, inspired by Windows.
